In order to make sure you have all the requirements for this project, run : pip install -r requirements.txt

In order to run this project locally use the command: python find_bloom_temp.py --data-source "your/data/location".

This project uses the open-meteo.com API in order to get weather data, so internet connection is required.